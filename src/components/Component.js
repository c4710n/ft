class Component {
  constructor() {
    this.added = false
    this.meta = {}
  }

  // eslint-disable-next-line
  onAdded(displayObject) {}

  // eslint-disable-next-line
  onRemoved(displayObject) {}
}

export default Component
